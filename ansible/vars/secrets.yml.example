# Copy this file to secrets.yml, fill in your values, then encrypt:
#   ansible-vault encrypt vars/secrets.yml

tailscale_auth_key: "tskey-auth-changeme"
discord_bot_token: "changeme"
openclaw_gateway_token: "changeme-generate-with-openssl-rand-hex-32"

# Optional overrides
local_network: "192.168.0.0/16"
openclaw_port: "4096"
